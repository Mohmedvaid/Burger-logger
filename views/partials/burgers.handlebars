{{#unless devoured}}
<div class="each-burger">
    <h3>Burger: {{burger_name}}</h3>
    <div class="btns">
        <button type="button" class="btn btn-success">Devoure</button>
    </div>
</div>
{{/unless}}