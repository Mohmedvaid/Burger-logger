{{#unless devoured}}
<div class="each-burger">
    <h3>Burger: {{burger_name}}</h3>
    <div class="devoure-btn">
        <button type="button" id={{id}} class="dev-btn btn btn-success">Devoure</button>
    </div>
</div>
{{/unless}}